## Gives info of the Project 
# Intro * project structure
# Instructution to download
# lience-MiT etc..
# libraries and frameworks used  /tech stack used

To Install dependecies we use command 
 npm i library-name/modulename;

dependecies: are the libraries/framework used for running application on development server * production server (live app)

To Install dev dependecies we use command 
 npm i library-name --save-dev;

 dev dependecies : are the libraries/framework used for running application on development server
 example :testing with jest ,eslint 

To Install global dependecies we use command 
npm i -g library-name/module
